name: data_reliability_lab
version: 1.0.0
config-version: 2
profile: data_reliability_lab

flags:
  require_explicit_package_overrides_for_builtin_materializations: false

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
clean-targets: ["target", "dbt_packages"]

models:
  data_reliability_lab:
    staging:
      +materialized: view
    marts:
      +materialized: table

seeds:
  data_reliability_lab:
    +quote_columns: false
